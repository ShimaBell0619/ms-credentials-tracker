{
  "name": "ms-credentials-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm ci --prefix api --ignore-scripts",
    "dev": "vite",
    "check": "biome lint .",
    "typecheck": "tsc --noEmit && npm run typecheck --prefix api",
    "test": "node --test tests/*.test.ts api/tests/*.test.ts",
    "build": "vite build",
    "build:function": "npm run build --prefix api",
    "postbuild": "node scripts/verify-pages-build.mjs",
    "preview": "vite preview",
    "test:e2e": "playwright install --with-deps chromium && playwright test"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@playwright/test": "1.62.1",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.7",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
