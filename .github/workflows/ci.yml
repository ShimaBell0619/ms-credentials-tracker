name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    uses: ShimaBell0619/web-app-foundation/.github/workflows/web-ci.yml@5382fc2c0735ce82c54dd05c07cb369d4b3b536a
    with:
      node_version_file: .node-version
      cache_dependency_path: package-lock.json
      run_test: true
      run_e2e: true
